import 'package:flutter/material.dart';

class UniversidadePage extends StatelessWidget {
  const UniversidadePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
