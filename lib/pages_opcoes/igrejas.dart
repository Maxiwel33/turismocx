import 'package:flutter/material.dart';

class IgrejasPage extends StatelessWidget {
  const IgrejasPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
