import 'package:flutter/material.dart';

class BenhosPage extends StatelessWidget {
  const BenhosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
