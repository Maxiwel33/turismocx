import 'package:flutter/material.dart';

class PracasPage extends StatelessWidget {
  const PracasPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
