import 'package:flutter/material.dart';

class PaginaDestaque extends StatelessWidget {
  const PaginaDestaque({Key? key, required this.size}) : super(key: key);

  final Size size;

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      child: Column(
        children: [],
      ),
    );
  }
}
